# ── Core API ──────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-dotenv>=1.0
requests>=2.31

# ── Frontend ──────────────────────────────────────────────
streamlit>=1.31

# ── Kubernetes client ─────────────────────────────────────
kubernetes>=29.0

# ── Vector DB (free, open-source) ─────────────────────────
chromadb>=0.4.24

# ── Embeddings (free, local model) ────────────────────────
sentence-transformers>=2.5.0

# ── RAG utilities ─────────────────────────────────────────
langchain>=0.1.0
langchain-community>=0.0.20
langchain-text-splitters>=0.0.1

# ── Airflow (DAG development; workers use Docker image) ───
apache-airflow>=2.8.0
apache-airflow-providers-docker>=3.9.0
openlineage-airflow>=1.7.0

# ── Lineage / Marquez client ─────────────────────────────
marquez-python>=0.38.0

# ── Data processing (used in DAGs) ────────────────────────
pandas>=2.1.0
pyarrow>=14.0.0
great-expectations>=0.18.0
scikit-learn>=1.4.0

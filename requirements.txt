# ── Core API ──────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-dotenv>=1.0
requests>=2.31

# ── Frontend ──────────────────────────────────────────────
streamlit>=1.31

# ── Kubernetes client ─────────────────────────────────────
kubernetes>=29.0

# ── Vector DB (free, open-source) ─────────────────────────
chromadb>=0.4.24

# ── Embeddings (free, local model — ~80 MB) ──────────────
sentence-transformers>=2.5.0

# ── Data processing (used in DAGs and scripts) ───────────
pandas>=2.1.0
pyarrow>=14.0.0
scikit-learn>=1.4.0

# ── Testing ──────────────────────────────────────────────
pytest>=8.0
httpx>=0.27

google-cloud-storage>=2.10.0